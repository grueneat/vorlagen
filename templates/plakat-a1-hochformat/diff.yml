# Visual-diff tolerance for the Plakat A1 reproduction.
# See docs/diff-tolerance.md for schema.
#
# A1 poster has only headline text + small impressum; at fuzz=25% the page
# is byte-clean against the baseline.
visual_diff:
  max_pixel_mismatch_pct: 1.0
  fuzz_pct: 25.0
