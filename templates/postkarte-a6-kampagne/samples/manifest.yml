slug: postkarte-a6-kampagne
# Production template — clean SLA (template.sla) keeps empty image slots
# for end-user injection. Gallery preview SLA (template-preview.sla) is
# generated by build_preview() with library demo content via the central
# image library (issue #13 D3).
#
# JSON content slots (klimaschutz.json, wohnen.json) sit alongside this
# manifest and remain unchanged.
uses_from_library:
  - id: themen_klimaschutz_solar
    anname: "P1 Hero"
    slot: 84x127mm portrait hero (front, page 0)
    target_sla: template-preview.sla
