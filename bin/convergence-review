#!/usr/bin/env python3
"""bin/convergence-review — classify + leverage-sort audit issues (issue #38).

See tools/convergence_review.py for the implementation.
"""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "tools"))
from convergence_review import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main(sys.argv[1:]))
